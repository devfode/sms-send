// Top-level build file where you can add configuration options common to all sub-projects/modules.

// This is an empty build script as all configuration is managed in app/build.gradle.kts
// and plugin versions are managed in settings.gradle.kts